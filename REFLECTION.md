| Категорія     | Що змінилось                           | Деталі                                     | Причина                                                                                           |
| ------------- | -------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| **Додано**    | Унікальний `id` для витрати            | Додано поле `"id": str(uuid.uuid4())`      | Дає можливість однозначно ідентифікувати записи (важливо для редагування/видалення в майбутньому) |
| **Додано**    | Імпорт `uuid`                          | `import uuid`                              | Потрібен для генерації унікальних ідентифікаторів                                                 |
| **Додано**    | Опціональна дата при створенні витрати | параметр `date` в `add_expense`            | Дає можливість додавати витрати заднім числом                                                     |
| **Додано**    | Валідація дати                         | `datetime.strptime(...)`                   | Запобігає некоректному формату дат                                                                |
| **Додано**    | Функція `filter_expenses`              | Фільтрація за категорією та датами         | Розширює функціональність для аналізу витрат                                                      |
| **Додано**    | Функція `display_expenses`             | Табличний вивід списку витрат              | Покращує UX при перегляді даних                                                                   |
| **Додано**    | CLI команда `list`                     | Новий сабпарсер з фільтрами                | Дає можливість переглядати витрати, а не тільки звіт                                              |
| **Додано**    | Фільтри CLI                            | `--category`, `--start-date`, `--end-date` | Гнучкий пошук і аналіз витрат                                                                     |
| **Додано**    | Форматування суми                      | `$` + формат з комами                      | Покращує читабельність                                                                            |
| **Змінено**   | Сигнатура `add_expense`                | Додано параметр `date`                     | Підтримка кастомної дати                                                                          |
| **Змінено**   | Структура витрати                      | Додано поле `id`                           | Розширення моделі даних                                                                           |
| **Змінено**   | CLI `add`                              | Додано аргумент `--date`                   | Дає користувачу більше контролю                                                                   |
| **Змінено**   | `main()` логіка                        | Додано обробку `list`                      | Розширення CLI інтерфейсу                                                                         |
| **Змінено**   | Валідація введення                     | Перевірка дат у `add` і `list`             | Захист від помилок користувача                                                                    |
| **Змінено**   | Вивід списку                           | Замість простого `print` — таблиця         | Краща структурованість                                                                            |
| **Прибрано**  | Нічого критичного                      | Усі старі функції збережені                | Зворотна сумісність збережена                                                                     |
| **Покращено** | UX CLI                                 | Більше команд і опцій                      | Робить інструмент ближчим до реального застосунку                                                 |
| **Покращено** | Масштабованість                        | Наявність `id` і фільтрації                | Підготовка до CRUD-операцій                                                                       |
| **Покращено** | Читабельність коду                     | Чіткіші функції (`filter`, `display`)      | Краща підтримка і розширення                                                                      |
